services:
  # ─────────────────────────────────────────────
  # Flink JobManager
  # ─────────────────────────────────────────────
  flink-jobmanager:
    image: flink:2.2.0-scala_2.12
    container_name: flink-jobmanager
    command: jobmanager
    ports:
      - "8081:8081"    # Flink Web UI
    volumes:
      - ./flink-conf/config.yaml:/opt/flink/conf/config.yaml   # 只挂载 config.yaml，不覆盖镜像内其他配置
      # flink-lib 不挂载 /opt/flink/lib！空目录会覆盖镜像内核心 JAR
      # Week 2+ 需要 JDBC connector 时，将在 backend/Dockerfile 里 COPY 处理
    networks:
      - flinkpilot-net
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8081/overview || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  # ─────────────────────────────────────────────
  # Flink TaskManager
  # ─────────────────────────────────────────────
  flink-taskmanager:
    image: flink:2.2.0-scala_2.12
    container_name: flink-taskmanager
    command: taskmanager
    depends_on:
      flink-jobmanager:
        condition: service_healthy
    volumes:
      - ./flink-conf/config.yaml:/opt/flink/conf/config.yaml
      # flink-lib 不挂载 /opt/flink/lib！见 flink-jobmanager 注释
    networks:
      - flinkpilot-net

  # ─────────────────────────────────────────────
  # Flink SQL Gateway（Session Mode）
  # 官方文档：使用与 JobManager 相同镜像，启动 sql-gateway 命令
  # ─────────────────────────────────────────────
  flink-sql-gateway:
    image: flink:2.2.0-scala_2.12
    container_name: flink-sql-gateway
    command: sql-gateway.sh start-foreground
    ports:
      - "8083:8083"    # SQL Gateway REST API
    depends_on:
      flink-jobmanager:
        condition: service_healthy
    volumes:
      - ./flink-conf/config.yaml:/opt/flink/conf/config.yaml
      # flink-lib 不挂载 /opt/flink/lib！见 flink-jobmanager 注释
    networks:
      - flinkpilot-net
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8083/v1/info || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  # ─────────────────────────────────────────────
  # PostgreSQL（业务数据库 + LangGraph 状态持久化）
  # ─────────────────────────────────────────────
  db:
    image: postgres:15-alpine
    container_name: flinkpilot-db
    environment:
      POSTGRES_DB: flinkpilot
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-secret}
    ports:
      - "5433:5432"    # 宿主机 5433→容器 5432；5432 被其他容器占用故用 5433
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - flinkpilot-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d flinkpilot"]
      interval: 10s
      timeout: 3s
      retries: 5

  # ─────────────────────────────────────────────
  # Agent 后端（FastAPI + LangGraph）
  # 开发期间注释掉，直接 uvicorn 启动更方便热重载
  # ─────────────────────────────────────────────
  # backend:
  #   build: ./backend
  #   container_name: flinkpilot-backend
  #   env_file: .env
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #     flink-sql-gateway:
  #       condition: service_healthy
  #   networks:
  #     - flinkpilot-net

networks:
  flinkpilot-net:
    driver: bridge

volumes:
  postgres-data:

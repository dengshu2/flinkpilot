# 本地开发覆盖配置（宿主机直接运行 uvicorn 时使用）
# 将 Docker 内部域名替换为 localhost，对应 docker-compose 映射的端口

DATABASE_URL=postgresql://postgres:secret@localhost:5433/flinkpilot
FLINK_REST_URL=http://localhost:8081
FLINK_SQL_GATEWAY_URL=http://localhost:8083
BACKEND_URL=http://localhost:8000
